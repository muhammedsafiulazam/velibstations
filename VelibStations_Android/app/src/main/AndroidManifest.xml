<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.muhammedsafiulazam.velibstations">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />


    <application
            android:name="com.muhammedsafiulazam.velibstations.MainApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <meta-data
                android:name="com.google.android.maps.v2.API_KEY"
                android:value="@string/google_api_key" />

        <uses-library android:name="android.test.runner" android:required="false" />

        <activity android:name="com.muhammedsafiulazam.velibstations.launch.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="com.muhammedsafiulazam.velibstations.feature.stationlist.view.StationListActivity"
                android:label="@string/stationlist_title"
                />

        <activity android:name="com.muhammedsafiulazam.velibstations.feature.stationinfo.view.StationInfoActivity"
                android:label="@string/stationinfo_title"
                />
    </application>

</manifest>